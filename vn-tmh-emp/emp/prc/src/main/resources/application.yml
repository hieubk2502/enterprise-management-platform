server:
  port: ${SERVER_PORT:8063}
  servlet:
    context-path: ${CONTEXT_PATH:/prc}
spring:
  application:
    name: vn.tmh.emp.prc
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:${SPRING_CLOUD_CONFIG_URL:http://localhost:8888}
  cloud:
    config:
      fail-fast: true
      name: ${SPRING_CLOUD_CONFIG_NAME:vn.tmh.emp.prc}

