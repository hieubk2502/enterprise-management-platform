server:
  port: ${SERVER_PORT:8065}
  servlet:
    context-path: ${CONTEXT_PATH:/hrm}
spring:
  application:
    name: vn.tmh.emp.hrm
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:${SPRING_CLOUD_CONFIG_URL:http://localhost:8888}
  cloud:
    config:
      fail-fast: true
      name: ${SPRING_CLOUD_CONFIG_NAME:vn.tmh.emp.hrm}

